allows you to signup for the newsletter. if done successfully show a success message else a failure message. at present, only runs on localhost:3000.
to run the code,
1. go to hyper
2. cd to directory path
3. run command : nodemon app.js
4. go to web and type url "localhost:3000"
